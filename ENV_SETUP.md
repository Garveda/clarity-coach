# Clarity Coach - Environment Setup

## OpenAI API Key Configuration

Create a `.env` file in the `backend/` directory with:

```
OPENAI_API_KEY=your-openai-api-key-here
```

**Important:** Never commit the `.env` file to version control!
